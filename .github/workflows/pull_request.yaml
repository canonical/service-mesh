name: Pull Request Checks

on:
  pull_request:
    paths:
      - 'terraform/**'
      - 'tests/**'
      - 'pyproject.toml'
      - 'tox.ini'
      - 'uv.lock'
      - '.github/workflows/pull_request.yaml'
      - '.github/workflows/_solution-level-tests.yaml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality-checks:
    uses: ./.github/workflows/_solution-level-tests.yaml
    with:
      istio-channel: "dev/edge"
